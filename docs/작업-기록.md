# 작업 기록 (운영 로그)

> 나중에 다시 따라갈 수 있도록 **무엇을·왜·어떻게** 바꿨는지 기록하는 파일.
> 새 작업을 할 때마다 맨 위에 날짜로 한 칸씩 추가한다.

---

## 2026-08-09 — 변호사별 분할 (v1.14.0~v1.16.0): 알림을 '사람 단위'로

### 배경 (사무실 요청)
- 팀 알림이 팀 전체 일정을 한 덩어리로 보내다 보니, 각자 자기 것을 골라내야 했다.
  → 팀 안에서 **변호사별로 나눠** 보고 싶다.
- 변호사마다 담당직원이 있으므로, 그 직원의 휴무도 해당 변호사 칸에 함께 보여야 한다.
- 오전 전체 알림도 한 통에 다 몰려 있어 길다. → **[기한]/[일정]/[휴무] 세 통**으로 분리.

### 조치
1. **`config/staff.yaml` 신설** — 변호사별 담당직원(주담당·부담당) 명단.
   최근 3주 실데이터의 `담당변호사 ↔ 담당직원` 조합으로 초안을 뽑고 사무실 확인을 받아 확정.
   휴무 일정 **제목의 이름**으로 배정하며, 한 직원이 여러 변호사 담당이면 그 섹션에 모두 실린다.
2. **팀 알림 변호사별 분할** (`build_team_message`): `# ○○ 변호사 내일 일정(…)` 아래
   [기한]/[일정]/[휴무] 세 칸. 정·수습 모두 개별 섹션, 비면 `없음`.
   공동담당은 담당 변호사 섹션에 모두, 어느 변호사에도 안 붙는 팀 일정은 맨 아래 `# 기타`.
3. **이돈호 변호사 개인 알림 독립** (v1.15.0): v1.14.0에서는 송무1·2팀 맨 위에 섹션을
   함께 실었으나 같은 내용이 세 번 나가 번잡 → 팀 알림에서 빼고 개인 알림 한 통으로.
   `teams.yaml` 의 `대표변호사` 항목은 제거하고, 발송 대상은 `main.py` `EVENING_TARGETS` 로 일원화.
4. **오전 알림 3분할** (`build_section_message`): 머리말에 섹션명을 붙이고(`📅 260810 월요일 [기한]`)
   빈 섹션도 `없음`으로 발송. @everyone 은 [일정] 한 통에만(알림 3연타 방지).
5. **수령·복사·등사는 담당직원 표기** (v1.15.0): 담당변호사가 여럿 달려 있어도 실제로는
   직원이 가므로 `담당(직원)` 맨 앞 사람으로 출석표기. 섹션 배정은 종전대로 담당변호사 기준.
6. **`&amp;` 노출 수정** (v1.15.0): 구글 캘린더가 넘겨주는 HTML 엔티티를 발송 직전에 해제.
7. **주말 묶음 보완** (v1.16.0): 이돈호 개인 알림도 금요일 저녁 토·일·월 묶음 대상에 포함.
   (v1.15.0에서 팀 알림에서 빠지면서 일·월 일정이 어디에도 안 나오던 빈틈을 메움)

### 검증
- 회귀테스트 67건 통과.
- 실데이터(2026-08-10) dry-run 결과를 사무실이 제시한 목표 양식과 **줄 단위로 대조해 일치 확인**.
- 메시지 길이는 팀별 1,000자 내외로 Discord 2,000자 제한 안(분할 발생 없음).

관련 커밋: `78c167a` (v1.14.0), `9d2eec0` (v1.15.0), 본 커밋 (v1.16.0)

---

## 2026-08-02 — 팀 개편 대응 (v1.13.0): 캘린더 재편 + 담당변호사 기준 분류

### 배경 (법인 조직 개편)
- 송무3팀 폐지(이종원→송무1팀, 채원협→송무2팀 흡수).
- Lawware 캘린더 재편: 송무1·2·운영팀이 **'운영팀' 캘린더 하나로 통합**(구글캘린더 이름 "송무그룹").
  계정1 소속 구 캘린더 6개는 삭제됨(404 확인). 계정2의 상담지원·기획전략·인사총무는 기존 유지.
- `#송무n팀` 담당직원 태그가 곧 삭제될 예정 → 태그 기반 팀 분류 불가.

### 조치
1. **팀 분류 기준 전환** (`transform.py` `event_in_team` 재설계):
   태그 대신 **담당변호사 소속 팀**(`config/teams.yaml`의 `변호사` 명단, 정변호사 1차 기준).
   담당변호사 중 정변호사가 하나도 없을 때만 `수습` 명단으로 2차 폴백.
   복수 팀 변호사 공동 담당이면 양쪽 팀 모두 알림. 교차출석 팔로우 유지.
   상담지원팀은 `#상담지원팀` 태그 또는 이돈호 담당/출석. 휴무는 제목 이름으로 판정.
2. **송무3팀 제거**: `main.py` EVENING_TEAMS·주말 묶음, `evening.yml` SONGMU3 웹훅 참조 삭제.
3. **캘린더 재연결**: 새 캘린더 3개(공용·수습TF·운영팀)를 서비스계정 1에 재공유(권한: 모든 일정 세부정보 보기)
   → GitHub Secret `GOOGLE_CALENDAR_ID`·로컬 `.env` 를 새 ID 3개로 교체. `GOOGLE_CALENDAR_ID_2` 는 무변경.
   cron-job.org 트리거는 시각·대상 동일 — 무변경.
4. **검증**: 회귀테스트 53건 통과, 실데이터 dry-run 대조([기한] 일정도 담당변호사 필드 보유 확인,
   직원 연차는 `담당(변호사):`에 팀 변호사가 달려 있어 팀 알림 포함 유지),
   Actions 수동 실행으로 오전(daily)·오후(evening, `date=2026-08-03`) 실전 발송 확인.

관련 커밋: `dc1e6a2` (v1.13.0)

---

## 2026-06-18 — 알림 양식 개선 4건 (머리말·섹션 라벨)

1. **오후 팀별 머리말 한 줄화**: 기존 2줄(`[팀] 내일 일정` + `📅 260618 목요일`)을
   `📅 [팀] 내일 일정(260618, 목)` **한 줄**로 합침(`format_header_lead()` 신설). 오전 단일
   알림(lead 없음)은 기존 `📅 260618 목요일` 그대로 유지.
2. **`[마감]` → `[기한]`**: 종일 사건 기한 섹션 라벨 명칭 변경(코드·주석·README 일괄).
3. **`[일정]` 라벨 추가**: 기한 외 일반 일정 묶음 위에 `[일정]` 라벨을 **항상** 표시
   (기한 없는 날에도). 빈 날은 종전대로 `일정 없음`.
4. **`[휴무]` 섹션 신설**: 휴가·반차·반반차·휴무·연차를 제목 키워드(`LEAVE_KEYWORDS`)로 골라
   종일/시간 구분 없이 **맨 아래 `[휴무]`** 에 모아 나열. `is_leave()` 신설.
   - 섹션 순서: `[기한]` → `[일정]` → `[휴무]`.
   - 주의: 휴무 판별은 **제목 키워드**라, 제목에 '연차/휴가' 등이 우연히 들어간 비-휴무 일정도
     휴무로 분류될 수 있음(예 '연차보고서 검토'). 실제 운영 캘린더엔 그런 사례가 거의 없어 그대로 둠.

테스트: `test_transform.py` 에 `[기한]+[일정]` 조합, `[휴무]` 분리, 휴무-only 날 케이스 추가(30건 전부 통과).

---

## 2026-06-16 — 오후 알림 17:30 변경 · 타팀 출석 팔로우 · 담당변호사 단독 폴백

### 1) 오후(내일 일정) 알림 시각 17:45 → 17:30
- cron-job.org의 오후 작업 스케줄을 **KST 17:30** 으로 변경(외부 스케줄러에서 직접 설정).
- 저장소 반영: `evening.yml` 주석 cron(`30 8 * * *` = UTC 08:30 == KST 17:30),
  `외부-스케줄러-설정.md`·`전체-구성도.md`·본 문서의 **현재 시각 표기**를 17:30으로 갱신.
  (과거 지연 사례의 "17:45→20:17" 일화는 당시 사실 기록이라 그대로 둠.)

### 2) 타팀 변호사 대신출석 → 그 변호사 팀 알림에도 포함
- 다른 팀 사건에 우리 팀 변호사가 대신 출석하면, 그 변호사 소속 팀의 '내일 일정'에도 일정이 뜨도록 함.
- `config/teams.yaml`(팀→소속 출석변호사 명단) 신설. `event_in_team()` 이 담당직원 태그 외에
  **출석변호사(없으면 담당(변호사))의 소속 팀**도 함께 매칭. 표시는 따로 붙이지 않고 그대로 추가.

### 3) 출석변호사 미기재 + 담당변호사 단독 → 담당변호사가 출석으로 간주
- 배경: 2026-06-16 오전 알림에서 14:00 김현규 조사기일이 `미입회`로 표시됨. 원인은 발송 시점에
  **출석변호사 칸이 비어 있었고**(담당변호사 김수인만 기입) 코드가 출석변호사만 보기 때문(버그 아님, 데이터 누락).
- 조치: 출석변호사가 미기재(필드 없음/공백)이고 담당변호사가 **1명뿐**이면 그 담당변호사가 출석하는 것으로 표기.
  단, 출석변호사 칸에 `미입회` 등을 **명시**하면 폴백 없이 그대로 미입회. 담당변호사 2명 이상이면 폴백 안 함.

---

## 2026-06-14 — 알림 지연 해결: 외부 스케줄러로 전환

### 문제
- 오전 알림(예약 07:00)이 **08:11**에, 오후 알림(예약 17:45)이 **20:17**에 도착. 정시에 안 옴.

### 원인
- 코드/설정 문제가 아님. **GitHub Actions의 `schedule:` cron은 정시 실행을 보장하지 않음**(공식 best-effort).
  전 세계 작업이 정시에 몰리면 큐가 밀려 수십 분~수 시간 지연됨.

### 해결 (선택: 외부 스케줄러 트리거)
1. **외부 무료 스케줄러 `cron-job.org`** 가 매일 정시(KST 07:00 / 17:45)에
   GitHub API `workflow_dispatch` 를 호출해 워크플로를 깨우도록 변경.
   - 외부 서비스는 GitHub 큐와 무관하게 정시 동작 → 지연 거의 사라짐.
2. **중복 발송 방지**: 두 워크플로(`daily.yml`, `evening.yml`)의 GitHub `schedule:` cron을
   **주석 처리(비활성화)**. `workflow_dispatch` 만 남김.
3. 설정 가이드 문서 작성: [`외부-스케줄러-설정.md`](./외부-스케줄러-설정.md)
4. 전체 구성도 작성: [`전체-구성도.md`](./전체-구성도.md)

관련 커밋: `a422959` (schedule cron 비활성화 + 외부 스케줄러 트리거로 전환)

### 트러블슈팅 기록 — cron-job.org 설정 중 404
- 증상: cron-job.org 작업 결과에 `{"message":"Not Found","status":"404"}`.
- 원인: **URL이 아니라 토큰(인증) 문제.** GitHub은 토큰이 리소스 접근 권한이 없으면
  403이 아닌 **404** 를 돌려줌(존재 여부 은폐 목적).
- 확인한 사실:
  - 저장소 `nova-law-legal/legal` = 공개, 기본 브랜치 `main`.
  - 워크플로 `daily.yml`(id 295211411) / `evening.yml`(id 295253972) 모두 active. → **URL 정상.**
- 해결: Fine-grained PAT의 권한을 **Actions: Read and write** + 대상 저장소 포함으로 맞추고,
  `Authorization: Bearer <토큰>` 헤더를 정확히 설정.
- 검증: 토큰 직접 테스트 시 응답 헤더에 `X-RateLimit-Limit: 5000`(인증됨),
  `x-accepted-github-permissions: actions=write`, 토큰 만료 2027-06-13 확인 → 정상.
- 결과: **두 워크플로 모두 `workflow_dispatch`로 실행 성공**(2026-06-14 13:05, 13:06 KST).

### cron-job.org 비용
- **완전 무료.** 신용카드 불필요. 하루 2회 호출은 무료 한도 내.
- 무료 제약: 최소 간격 1분(무관), 요청 제한시간 ~30초(우리는 트리거만 보내고 끝나 무관), 유료 SLA 없음.
- 보안: 이 서비스에 GitHub 토큰이 저장되므로 토큰 권한은 최소(저장소 1개 + Actions write)로 유지.

---

## 현재 운영 상태 (2026-08-09 기준)

| 항목 | 상태 |
|---|---|
| 오전 알림 | `daily.yml`, 외부 트리거 KST 07:00, 오늘 일정 전체 → **[기한]/[일정]/[휴무] 3개 메시지**(@everyone 은 [일정]에만) → 기본 채널 |
| 오후 알림 | `evening.yml`, 외부 트리거 KST 17:30, 내일 일정 → **이돈호 개인 + 송무1·2팀 3개 메시지**(전용 웹훅 미설정이라 기본 채널로 폴백) |
| 팀 알림 구조 | 팀 안에서 `# ○○ 변호사 내일 일정(…)` 섹션으로 분할, 변호사마다 [기한]/[일정]/[휴무] 세 칸(비면 `없음`) — v1.14.0~v1.16.0 |
| 팀 분류 | 담당변호사 소속 팀 기준(`config/teams.yaml`, 수습 폴백) — v1.13.0 |
| 변호사별 배정 | [휴무]는 본인 + `config/staff.yaml` 담당직원(주·부담당)의 휴무 — v1.14.0 |
| 주말 묶음 | 금요일 저녁에는 세 알림 모두 토·일·월 3일치를 한 메시지로(`WEEKEND_BUNDLE_TARGETS`) — v1.16.0 |
| 캘린더 | 계정1: 공용·수습TF·운영팀(송무그룹) / 계정2: 상담지원·기획전략·인사총무 |
| GitHub `schedule:` cron | **비활성화(주석)** — 외부 트리거 단독 운영 |
| 트리거 수단 | cron-job.org (무료) → GitHub API `workflow_dispatch` (Fine-grained PAT) |
| 검증 | 수동 dispatch 실행 성공 확인 완료 (2026-08-02 오전·오후 실전 발송) |

---

## 나중에 확인/할 일 체크리스트

- [ ] cron-job.org 두 작업의 **타임존이 Asia/Seoul(KST)** 인지 확인 (UTC면 9시간 어긋남)
- [ ] 실제 스케줄 시각(07:00 / 17:30)에 정시 도착하는지 며칠 관찰
- [ ] GitHub PAT 만료일(**2027-06-13**) 다가오면 갱신하고 cron-job.org 헤더 토큰 교체
- [ ] 외부 트리거가 죽는 만일에 대비하려면, 워크플로의 주석된 `schedule:` 을 백업으로 되살릴 수 있음
      (단 이때는 중복 발송 방지 로직 필요 — [`외부-스케줄러-설정.md`](./외부-스케줄러-설정.md) 마지막 절 참고)

---

## 참고 문서
- [`전체-구성도.md`](./전체-구성도.md) — Lawware→Discord 전체 데이터 흐름 다이어그램
- [`외부-스케줄러-설정.md`](./외부-스케줄러-설정.md) — cron-job.org 설정 단계별 가이드
- 루트 [`README.md`](../README.md) — 봇 구성·변환 규칙·최초 설정
